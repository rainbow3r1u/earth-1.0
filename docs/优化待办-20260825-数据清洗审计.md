# 地球1.0 系统优化待办（2026-08-25 数据清洗审计产出）

> 来源：2026-08-25 幽灵模式全库排查 + 训练数据清洗链路审计（会话记录见 memory）
> 状态规则：每项独立 A/B 实验验证后才可上生产（铁律1：180d 回测对比 + 用户审核）
> 优先级排序依据：预期收益 × 验证成本

---

## ⭐ LONG侧改造第一推进方案：横截面排名特征 + 大盘 regime 特征（2026-08-26 立项）

**证据链（2026-08-26 分析）**：
- 8/8 起 IC_L 持续为负（17天中14天），但 AUC_L 已回基线（8/24 单日 0.690）——**分类能力活着、排序死了**
- 分桶分解：LONG 头部桶（top1-10）14日均 +0.49% vs 宇宙均值 +1.00%，排序 alpha 为负；SHORT 头部桶 -5.59% 教科书级正确
- 8/18-20 暴拉期梯度完全倒挂（8/19 尾部 +11.97% vs 头部 +0.86%）——**轮动补涨行情，模型排序编码动量风格，反向挨打**
- 真实盈亏：8/8 起 17 天 LONG 170笔 +63.4U（0.37U/笔）靠 beta+机制续命，SHORT 170笔 +234.2U 贡献 79% 利润

**病根**：模型只看单币自身时序特征（900+ 全是 RSI/动量/OI 等），**看不见自己在宇宙里的相对位置**。"上月输家补涨"是横截面信息，任何单币时序特征都编码不了。

**改法（特征层，不动标签不动损失函数）**：
1. 横截面排名特征组：每币的 7d/30d 收益宇宙 rank、成交额热度 rank、换手 rank、距 30d 高点回撤 rank（低 rank = 超跌补涨候选）
2. 大盘 regime 特征组：BTC 30d 已实现波动率、BTC 距前高、宇宙动量因子 5 日滚动值（让模型学"暴拉期谁补涨"）

**注意**：现有宏观特征（SP500/DXY）周末缺失是**休市事实，不是数据缺失**——周末填 0 在"市场关闭"语义下可辩护（该不该 ffill 归 P2 独立验证，与本方案无关）。本方案的 regime 特征用 crypto 原生数据（BTC/宇宙），7×24 无休市问题。

**验证**：GPU 180d A/B（XS_RANK 臂），对比实验前清缓存 `rm -rf by_day_cache/*`，TOP1 参数不动。
**后续队列**（本方案出结果后依次推进）：A 残差化标签（超额收益标签，掐 beta 病根，全链路改动最大）→ C LambdaRank 排序损失（直修 IC，下游语义需重校准）→ D 新数据源（taker ratio/大户持仓/orderbook 失衡，先探 Binance 历史深度）。

**2026-09-01 用户定性 + 证据补强（标签层方案的权重上调）**：
- 确认事实：LONG/SHORT 模型**输入特征完全相同**（同一份 X_train、同一套 winsorize、同超参，仅标签/seed/scale_pos_weight 不同）——两侧行为差异全部来自"标签分布"与特征的关系结构
- 新证据（8/29-8/30）：LONG 标签的脆弱性再次上演——IC_L -0.17/-0.29、AUC_L 0.41，8/30 LONG 10笔全灭（-158.8U，含 86% 置信度的 BMT），同期 SHORT 7 笔止盈 +129.9U
- 机制解释：LONG 标签 = "48h 涨过+5%"，在普涨/轮动市里大量**beta 假阳性**（垃圾币跟涨也命中），模型学到的是"高波动高热度"而非真 alpha；SHORT 标签 = "独立暴跌"，币种特异性强、天然免疫大盘
- **用户结论（9/1）：LONG 确实需要优化标签信息才能适应这个市场**
- 优先级影响：后续队列中"A 残差化标签"从第 3 位实质上浮——特征层方案（本条）治"看不见横截面"，标签层方案（A）治"beta 假阳性"，两者互补；若 XS_RANK 臂 A/B 显示 LONG 改善有限，标签残差化就是下一张牌

---

## ⭐⭐ 系统利润周期与 BTC 波动 regime 绑定（2026-08-28 确立，核心认知）

**证据链（2026-08-28 分析，30 天样本）**：
- BTC 5日已实现波动 vs AUC_L：**r=-0.52, t=-3.79**（显著负相关）；vs AUC_S：r=-0.36（不显著）
- 高波动天（>2%，n=7）AUC_L 均值 0.491 vs 低波动天（≤2%，n=23）0.660——**LONG 侧对波动敏感，SHORT 侧基本免疫**
- 8月完整样本：18 天平静期（vol 0.3-1.5%）双侧吃 alpha → 4 天冲击期（vol 2.7-3.6%）LONG AUC 崩至 0.24、SHORT 侧暴拉普跌中 AUC_S 反冲 0.92 → 回平静期（vol 1.1%）双侧恢复
- 两侧利润**都产自平静市**：LONG 吃横盘温和 beta+机制钱（0.37U/笔），SHORT 吃横盘里独立选币的暴跌捕获（8/3-8/18 贡献几乎全部利润 +996% 端点基数）；波动大市两侧都挨打（LONG 挨轮动反转、SHORT 挨逼空）

**本质**：这个系统吃的就是横盘期 alpha。BTC 波动率是利润周期的主时钟：

| regime | BTC 5日vol | 系统状态 | 动作 |
|---|---|---|---|
| 平静期（吃 alpha 窗口） | ≤1.5% | 双侧正常 | 全档运行 |
| 波动期（挨打/等待） | >2% | LONG 分类力崩、SHORT 独立alpha仍可 | 降 LONG 档、SHORT 保持、SL 兜底 |
| 恢复期 | 回落中 | AUC_L 谷底后连续回升 2-3 天 | 观察恢复 |

**D+2 AUC 确认信号的正确预期**（时序物理）：48h 标签 → 质量确认最早 D+2 早报。对隔日快冲击是追认（8/18 的 AUC 崩实际是被窗口内的 8/19 暴拉打崩，非领先信号）；**对持续 5 天+ 冲击有真实价值**——第 2 天确认后剩余天数全程降档（8月案例：若 8/20 早报降 LONG，8/19-8/21 三天半仓）。crypto 冲击多为持续数日，急跌一天结束的是少数。

**三层信号链与操作手册（2026-08-28 与用户共同确立，晨报 5.5 节已全部自动化）**：

| 层 | 信号 | 管什么 | 提前量 | 动作 |
|---|---|---|---|---|
| 1 | 🟠 IC 领先预警（IC_L5<0 且 AUC 好 且 IC 仍在下行） | 慢冲击（5 天+） | **~8 天**（n=1） | 开始留意 LONG 档位 |
| 2 | BTC vol 变色（绿→黄→红） | 中速冲击 | 0~2 天 | 黄=警戒 红=预期 AUC 将崩 |
| 3 | AUC D+2 确认（单日<0.55 / 5日均跌破 0.60） | 冲击兑现盖章 | 追认 | 降 LONG 档、SHORT 保持 |
| 4 | SL 5% | 快冲击（隔夜爆） | 实时 | 兜底，无需判断 |

**四灯驾驶舱（2026-08-28 上线，commit 5dc6dee，用户定级为系统最高价值仪表）**：
IC 排序（风格）/ BTC 波动（行情）/ AUC 分类（确认）/ PERM 过拟合（模型本体）——每灯管一种病，全绿=模型最佳状态。
**仪表与动作的边界（用户纪律，2026-08-28 确立）**：
- **四灯是决策主仪表，盈亏是验证指标**——四灯是"钱的因"（系统当前处于哪个赚钱状态），盈亏是"钱的果"（噪声大，BTR 单币一天 ±1024U 级）；盯盈亏做决策=看后视镜开车，盯四灯=看挡风玻璃
- 灯红提示警惕，但降档等人工拍板——在下一轮冲击验证"IC 领先 8 天"可复现之前（n=1），四灯是**高价值监测仪表**而非**自动开仓信号**
- 盈亏归因模糊（同样亏 100U 分不清"信号坏了"还是"正常回撤"），四灯归因清晰（红黄绿直接映射 LONG 档位/SHORT 砍留/不动）
- 10/23 评审时四灯状态序列是分段归因的依据（哪些天该降档没降）——邮件每天自动留痕

**判读口诀（用户总结）**：
- IC 是提前预警，AUC 是 2 天内的行情追加确认
- **IC 转正 ≈ 行情开始横盘 = alpha 窗口重开**（IC 负的本质是均值回归惩罚动量排序，IC 修复说明风格回归）
- 行情稳没稳，直接看 AUC：单日先回 0.55+，5 日均值再回 0.60
- 校准：IC 转正是横盘的充分非必要条件——小幅负但 AUC 好 = 横盘但信号弱，吃保守的钱（LONG beta+机制）
- IC 修复尾声 vs 恶化反弹的区分：看 IC_L5 的导数（回升=修复 🟢 / 下行=恶化 🟠）；修复尾声需 IC>-0.05 逼近转正才算
- 8/15-17 教训：信号会"翻面"（IC 一度逼近转正又二次探底）——滚动均值跟数据走是特性不是 bug，翻面当天自动修正

**已上线（晨报 4a4，commit 392aad2）**：7日批作业表加 BTCvol 列；AUC_L<0.55 且当日 vol≤2% → 🚨"8/18形态"提示（失效始于平静市）；当日 vol>2% → ℹ️冲击兑现期提示。
**已上线（晨报 5.5 节，commit 2a8af56）**：IC 领先预警三分态（🟠早期预警 / 🟢修复尾声 / 静默），回放验证 8/12 晨报准时触发、领先 AUC 崩 8 天。

**待办（本条目下的后续动作）**：
- [ ] 仓位节律规则化（需更多样本校准阈值）：AUC_L≥0.60 全档 → D+2 确认跌破 0.55 降 LONG 档 → 谷底后回升 2 天恢复观察 → 回稳 0.60 恢复全档
- [ ] 与 ⭐ LONG侧方案 regime 特征组（BTC 30d 已实现波动率）同源——让模型自己看见环境，比人看 D+2 报警更治本

---

## ⭐ MAE strict48 方案（2026-08-25 归档待复启 — 混合结构失败时的 Next Plan）

**记忆锚点**：对表清单 C.5 已写明"部分过 → 按判死线拆组件, **MAE 复启走 TOP20 形态**"；归档文档在 [experiments/mae_strict48/README_ACCEPTANCE.md](../experiments/mae_strict48/README_ACCEPTANCE.md)。

**触发条件**：混合结构 60 天验证（~2026-10-23 评审，判据见 docs/对表清单-20261023）任一判死线触发 → MAE 从"GPU 服务器开机"一行直接续跑，**预注册标准不变**（BSS≥0.25 / 4段walk-forward / 影子盘30天 / 小资金30天，禁止事后改门槛）。

**TOP20 复启形态（8/25 拍板）**：预测产出 TOP20 候选池（非 TOP10），MAE 分层二次筛选（θL=0.3 / θS=0.2 剔高 MAE 币换稳币）。扩池的逻辑：给 MAE 留替换空间——直接滤 TOP10 可能只剩 1-2 个存活者，随后被止损打掉，篮子失去盈利能力。

**当时的证据（未被证伪，仅让位于混合结构）**：BSS +0.41；分层 OOS Sharpe +156%；19 单元测试绿。8/25 归档原因：混合结构（LONG无TP+SHORT TP10/SL5）从**出场端**解决了同一"拿不住赢家"问题，两条路线互斥占用验证资源。

**2026-08-28 新增佐证（用户独立复盘得出，与 θS=0.2 殊途同归）**：
- 止损建议表扫损单（止损但方向对）51 笔：**80% 的 MAE≤20%**（中位 9.9% / P75 18.1% / 最大 52.7%）——"扛到 20% 就够、过了就是反转"的用户判断与 θS=0.2 分界吻合
- 反事实模拟：这 51 笔现 SL5% 合计 -255%；若 SL20% 扛得过拿终点 = +210%。但注意 SL20 物理不可行（10x 杠杆 ~10% 爆仓），**正确打开方式 = 入场前 MAE 过滤**（不开高预测 MAE 单），不是放宽止损线——全量 SL 敏感性扫描无稳定平台（SL12 +183% / SL15 +97% / SL20 +128% / SL25 +42%，纯路径噪声），且方向错单会陪扛（52笔 × -5→-20 = -518% 抵消大半）
- 晨报止损建议表已升级（commit 2ea3657/dfdfc8c）：黄底高亮扫损单 + 表尾汇总 MAE 中位/最大——10/23 评审时直接读表即可，无需重跑

**MAE 原理一句话**：事后 MAE = 48h 窗口内对持仓方向最不利的极值偏移（min/max 路径函数，100% 精确）；事前 MAE = GBDT 对该极值的条件分布建模（概率性，群体有效非逐单精确）。θ 过滤赚"筛掉群体高风险单"的钱。

---

# ⭐⭐⭐ 48h vs 72h 持有窗口终审 — 10/23 评审最高优先级议程（2026-09-02 预研完成）

## 问题根源（9/2 用户发现）
标签 = `next_ret = (closes[i+2]-opens[i])/opens[i]` = **72h 终点**语义（open[D]→close[D+2]=08:00 D+3）；
真实生产平仓（hold≥48h 仅每日08:05检查）实际 ≈71.7h，与标签对齐；
但全部影子结算（3.6/3.7/3.8/3.9）和 residual_live 实盘 = **strict 48h**。
→ **结算链比标签/生产/GPU回测少收 24h 右尾敞口**。

## 8/9 老结论的条件性（不可直接套用）
8/9 路径研究"48/72 差异极小(胜率差0.0pp)"成立条件 = **TP10/SL5 结构**（351/352笔在48h内已触发出场，第3天无敞口）。
当前 LONG 无止盈 + SHORT TOP10 池（尾部单常两个触发都够不着）→ 前提已变，老结论不适用。

## 9/2 重算实验（29 天前向数据，580 笔，300U 名义，1m 全费用重放）
原始明细：data/hold_48_72_experiment_20260902.log | 脚本：docs/hold_48_72_experiment.py

| 口径 | 48h | 72h | 差(72-48) |
|---|---|---|---|
| LONG（无止盈/SL5） | +2872.0U | +5118.5U | **+2246.5U (+78%)** |
| SHORT（TP10/SL5） | -85.5U | +769.1U | **+854.6U**（TP未触发的单拿72h终点更优） |
| **合计** | **+2786.6U** | **+5887.6U** | **+3101.0U (+111%)** |

**拆尾分析（用户核心关切"除开史诗级单日"）**：
- 仅剔 8/7 史诗日(+1666)：仍 **+1435U / +51U/天**（≈48h日利润的+18%）
- 再剔 8/25/8/23：+691U；前三大肥日贡献 78% → 右尾放大器本质
- 对称去尾（各剔3天）：+1329U / +58U/天
- 砍头5天：仅+121U；砍头8天：-366U
- 逐日胜率 17/29=59%，中位 +53.8U/天
- 代价面：延续跌势日多扛一根阴线（最差 8/5 -338U）

## 10/23 评审决策框架（届时执行）
1. 用残差臂 60 天 1m 数据重跑本实验（两臂互证，本实验=单臂预研）
2. 通过标准：双臂 72h 均占优 且 剔除头3日后仍 ≥+30%/天量级 且 逐日胜率≥55%
3. 迁移需三方联动：结算tracker + 晨报口径 一次性改 72h（标签本就是72h语义，迁移=消除错位而非引入新偏差）
4. 实盘资金重算：同时持仓 2批→3批（峰值20→30笔），保证金+50%，2x/5x下爆仓距离压缩需评估
5. 若通过 → **"标签72h/结算72h/实盘72h"三方对齐**；若不通过 → 维持48h，错位已知且已量化（≈-111%相对量级的理论损失上限参考）

**⚡ 2026-09-03 实况更新（用户决策）**：
- **residual_live 实盘执行器已提前迁移 72h**（HOLD_DAYS=3，守卫 60%→85% 支持三批满配 ~30 笔×8U=240U/81%），实盘从 9/3 起成为 72h 活体实验臂
- 存量仓位基于 pos['date'] 计算到期 → 自动延至 72h（9/2 批→9/5 到期，9/3 批→9/6 到期）
- **影子结算链（residual_tracker/晨报3.9）维持 48h 不动**，10/23 终审数据照旧 → 9/3~10/23 为两口径分叉期，实盘 vs 影子对照需注意窗口差 24h
- 10/23 若终审通过，仅需改结算 tracker + 晨报口径（实盘已完成）；若不通过，实盘需回滚 48h（HOLD_DAYS=2，一行）

## 已否决的过渡方案
- ❌ 48h+72h双轨结算同时展示（用户定：数据够了就做最后对比，不搞双轨）
- ❌ 反事实列写入晨报（用户定：不加，10/23 一次性终审）

---

## P0：训练/推理不对称（唯一"预期必然为正"项）— ✅ 2026-09-01 已验证：无实际收益，归档不采纳

**问题**：预测端剔除 5 日均成交额 <500,000U 的币（`auto_dual_trade.py:2020` MIN_VOLUME_24H），但**训练集零过滤**——日均 1 万 U 的死币照样参与训练。模型在低流动性分布上学到的模式，实盘永远用不到；高流动性币的样本被噪声稀释。回测无法暴露（回测执行过滤与生产一致，训练侧同样不过滤）。

**2026-09-01 GPU A/B 实测结论（VOLUME_FILTER 臂，口径=上次 RESL 实验完全一致：aligned/VOLRAW/FUND/180d/双窗）**：

| 臂 | 原窗 Sharpe/Cum | OFF30 Sharpe/Cum | 训练样本保留 |
|---|---|---|---|
| BASELINE | 21.33 / +1290% | 22.12 / +1310% | 100% |
| VF300 | 22.12 / +1310% | — | 99.93% |
| VF500（=P0 原方案） | 19.20 / +1230% | 22.12 / +1310% | 99% |
| VF1000 | 22.97 / +1330% | 25.92 / +1390% | 92% |
| **SEED+100（种子噪声）** | **20.43 / +1262%** | — | **100%（什么都没改！）** |
| **SEED+200（种子噪声）** | **17.96 / +1190%** | — | **100%（什么都没改！）** |

- **前提不成立**：≥90 K 线宇宙里 vol5<500k 的只有 23/536 币（4%），训练样本仅 1% 被滤（原预估 -30% 严重高估——90 天 K 线门槛已挡掉了绝大多数死币）
- **A/B 结果**：VF500 原窗 -2.13 Sharpe、OFF30 持平——P0 的"预期为正"实际为 **≈0**
- **种子噪声标尺（本实验最有价值的产出）**：同数据同参数只换 XGB 种子，Sharpe 在 **17.96~21.33 摆动（跨度 3.4）、Cum ±100pp、DD 19.9~35.8pp**。据此：
  - VF500 的 -2.13、VF1000 原窗的 +1.64 → 全在种子噪声带内，不可解读
  - VF1000 OFF30 的 +3.80 → 刚好压线，强度只有 RESL(+11.4) 的 1/3，不采纳
  - **方法论铁律（此后所有 A/B 适用）：单窗口 |ΔSharpe|<3.5 一律视为噪声；只有双窗一致且大幅超噪声带（如 RESL +11.4/+4.8）才算信号**
- **选币敏感度副产品**：改 0.07%~1% 训练样本 → 44% 天数的 TOP1 选币翻转——TOP1 概率常年贴近临界值，印证 TOP10 组合设计正确，也解释了单日盈亏噪声（BTR ±1024U/日）
- 代码臂保留在 gpu_backtest_exp.py（VOLUME_FILTER / SEED_OFFSET 环境变量），生产不采纳

---

## P1：OI 特征半程失明

**问题**：`fetch_oi_for_symbols` 只取最近 90 个 OI 快照（`auto_dual_trade.py:783` 的 `[:90]`），但训练窗 180 天 → 前半程样本 oi_chg 恒为 0，且"缺数据"与"OI 真没变"不可区分。oi_daily.json 本身是累积合并的（历史更长），截断疑似 Binance API limit=90 的被动继承，无注释无实验。

**改法**：放开 `[:90]` 截断，取全量历史。
**验证**：GPU 180d A/B（注意 oi_chg 特征分布前后对比，确认前半程不再恒零）。
**风险**：低——纯数据覆盖扩展，不动任何逻辑。

---

## P2：缺失值语义混淆（宏观特征周末归零）

**问题**：宏观/板块/ETF/费率缺失一律填 0（`daily_predictor.py:527-552`，无 ffill），NaN 也填 0。SP500/DXY 每个周末整列变 0 → "无数据/中性/零收益"三态不可分。
**用户澄清（2026-08-26）**：SP500/DXY 周末缺失是**休市事实**，不是数据缺失——周末本就没有交易。填 0 的问题不是"数据丢了"，而是"休市（应延续周五收盘状态）"和"宏观真崩了（0）"两个语义被压成同一个值，模型无法区分。

**改法**（二选一，A 成本低）：
- A：缺失时 ffill 前值（周末宏观=周五收盘值）
- B：加缺失指示位特征（维度 946→+宏观组数量，涉及 EXPECTED_N 断言，改动大）

**验证**：GPU 180d A/B（MACRO_FFILL 臂）。

---

## P3：拍脑袋阈值群（P0-P2 出结果后逐个验证）

| 阈值 | 现值 | 位置 | 问题 |
|---|---|---|---|
| 标签异常线 | \|next_ret\|>5.0 (±500%) 才剔除 | auto_dual_trade.py:944 | 过宽：2日+400% meme 币照样进训练。建议试 ±100%~200% |
| 正样本门槛 | pos≥5 | auto_dual_trade.py:1080 | 与单模型版 pos≥10 (daily_predictor.py:1367) 不一致，无实验 |
| 热身期 | i≥25 天 | auto_dual_trade.py:851 | 无注释 |
| 0 成交量天 | 不剔除 | — | q=0 的 K 线留在特征里 |
| 极端值线 | \|x\|>1e6 仅统计 | auto_dual_trade.py:1053 | 阈值无实验（有思考注释） |

---

## 不动清单（有实验背书，动=开倒车）

- 90 天 K 线门槛（Sharpe 5.80 vs 400 天 2.30，实测）
- winsorize 0.1%/99.9%（1% 档曾把 LONG Sharpe 41.55→5.38，教训记录在案）
- 180 天训练窗口（参数搜索最优；与 90 天门槛/90 天特征不对齐是实测最优组合）
- 标签 ±5% / 48h 窗口（整个 Top1 参数体系的地基）

---

## 已完成（勿重复做）

- ✅ 2026-08-25 幽灵模式全库排查：3.96SHARPE_repo/daily_predictor.py L696 休眠残留已同步修复（.copy()），6 个 gpu_*.py 均安全，SAMPLECHK 探针连续 22 天正常
- ✅ 2026-08-25 COS 清理两轮（13.89GB→4.37GB，61.7 万对象删除，清单在 data/cos_deletion_manifests/）
- ✅ 混合结构影子臂（3.8 节）08:21 口径与 3.7 完全对齐，60 天验证至 ~10/23

---

# ⭐⭐ RESIDUAL 残差LONG方案 · 影子臂+小资金实盘 双轨上线（2026-09-01）

## 方案本质
LONG 标签的 beta 假阳性：普涨市里垃圾币跟涨也命中"48h涨过+5%"，模型学的是高波动高热度而非选币 alpha（8/30 主臂 LONG 10 笔全灭 -158.8U 实证）。
**残差标签 = 币 48h ret − 当日宇宙中位 ret > +5pp**（超出宇宙中位 5 个百分点才算正样本，把 beta 从标签里减掉）。
GPU 180d 双窗验证：原窗 Sharpe 32.75 / 换窗 OFF30 26.89 vs 基线 21.33/22.12；LONG 砍头 5 笔后 +1309%（基线 +870%）。
SHORT 标签不动（SHORT 命门在止损不在标签，TP10/SL5 已是最优解）。

## 三层架构（全部纯旁路，不碰实盘主路径）
| 层 | 文件 | cron | 职责 |
|---|---|---|---|
| 训练输出 | auto_dual_trade.py（90d28ba） | 08:05 主程序内 | 影子 XGB 同参训练，top10_long_residual 写入 pred 文件，+16~20s 不影响 08:23 时序 |
| 影子结算 | audit/residual_tracker.py | 08:55 | 结算规则与 hybrid_tracker LONG 完全一致（无TP/SL5%/48h/08:21/300U/1m全费用），存 residual_tracker.json |
| 晨报展示 | daily_digest_email.py 3.9 节 | 09:00 | 影子 vs 主臂LONG 逐日对照（当日差/累计差/当日选币TOP10） |
| 小资金实盘 | audit/residual_live.py（2fc418a） | 08:21 trade + 每小时:31 reconcile | 2000CNY≈297.6U，40U名义/5x逐仓/SL5%/48h，每日≤10笔 |

## 实盘资金纪律（用户确认 2026-09-01）
- 单笔名义 40U / 保证金 8U / 5x 逐仓；峰值 20 笔×8U=160U（54%），缓冲 137U
- 亏损上限：单批全灭 ≈ -20U；48h 双批全灭 ≈ **-41U（-15%）**；逐仓单笔最大亏 8U 不连坐
- 盈利预期：均值 ≈ +3U/日（回测口径），右尾日 +40~80U，逆风期（IC弱）头一周可能小亏——**不以首周论生死，裁判是 60 天累计到 ~10/23**
- 对账比例：**实盘 × 7.5 ≈ 影子臂**（40U vs 300U 名义）；差值 = 执行摩擦（入场滞后 2~5 分钟 + 滑点），是本测试最值钱的产出

## 上线时序（勿记，照表核对）
- 9/2 08:05：影子模型首次生产原生运行（核验 trade.log 两行："训练影子多头模型(残差标签)...pos=16671" / "[影子臂-残差] LONG TOP3"）
- 9/2 08:21：执行器首批开仓（核验：`python3 audit/residual_live.py status` 应见 10 笔在持+各自 SL；`tail -30 logs/residual_live.log`）
- 9/4 晨报 3.9 节：第一行盈亏数据（影子臂 + 实盘 history 均有）

## 本次踩坑记录（复用必读）
1. **by_day 是 5 元组** `(sym, feat, ll, ls, nr)`——解包元数写错是运行时崩溃（原 6 元素解包 08:05 必炸）
2. **nr 是百分数**（next_ret×100）——残差阈值是 `> 5.0` 不是 `> 0.05`，写错标签退化为"跑赢中位即正"（正样本率虚高 50%）
3. **条件单必须走 Algo Order API**（/fapi/v1/algoOrder，-4120 拒普通端点）；GET 查挂单用 openAlgoOrders，DELETE 按 algoId 用 /fapi/v1/algoOrders——币安 GET/DELETE 端点不对称是老坑，residual_live 已移植主程序全套 FIX
4. **income 时间戳秒级截断**：记账窗口要两侧加缓冲（-2s/+5s），否则开仓手续费落窗外、净额记 0
5. **离线重放保真度校验**：标签只依赖 K 线缓存可逐位重放；特征受盘中外部数据文件更新影响会有 ~0.0008% 差异（今日实测 698/8968 万），属正常，训练应改用生产存档矩阵
6. 离线补算 pred 时 pred_feats_{date}.npz + 当日 bounds 可完整复原 X_pred（"重放=生产"校验链）


## 附：清洗管线规则全景（审计日快照）

26 条规则清单及验证状态详见会话 memory（2026-08-25 审计）。核心流程：
```
采集(06:00) → 币种准入(K线≥90天) → 逐币逐日样本(25天热身, 标签48h±5%, |ret|>500%剔除)
→ 训练集组装(180天窗, NaN→0, 全零行剔除, 特征屏蔽Kronos/liq/ETF, winsor 0.1%)
→ XGBoost(d6-w1-L10-A10-s0.8-c0.6) + SOUP(近3日概率平均) → 预测端过滤(5日均量≥50万)
```
